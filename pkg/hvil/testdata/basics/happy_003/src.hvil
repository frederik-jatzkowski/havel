
func main() {
    declare (
        n : 4 byte
    );

    block entry {
        debug.dump(n);
        $a : 1 byte = 0b1;
        debug.dump($a);
    } => exit;

    block exit {
        debug.dump(n);
        $a : 1 byte = 0b1;
        debug.dump($a);
    } => return;
}