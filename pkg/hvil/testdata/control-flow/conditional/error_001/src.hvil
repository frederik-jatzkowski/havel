
func main() {
    declare (
        n : 4 byte
    );

    block entry {
        $check : 2 byte = 0b1;
    } if $check then print else exit;

    block print {
        debug.dump(n);
    } goto exit;

    block exit {
        debug.dump(n);
    } return;
}