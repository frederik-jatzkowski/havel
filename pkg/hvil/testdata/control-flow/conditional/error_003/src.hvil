
func main() {
    declare (
        n : 4 byte
    );

    block entry {
        $check : 1 byte = 0b1;
    } => if $check then print else a;

    block print {
        debug.dump(n);
    } => exit;

    block exit {
        debug.dump(n);
    } => return;
}