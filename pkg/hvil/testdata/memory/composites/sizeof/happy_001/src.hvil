

func main() {
    block entry {
        $basic : ref = mem.sizeof(8 byte);
        debug.dump($basic);

        $composite : ref = mem.sizeof({ 8 byte, 8 byte });
        debug.dump($composite);
    } return;
}
