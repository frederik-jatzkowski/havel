

func main() {
    block entry {
        $basic : 1 byte = mem.sizeof(8 byte);
        debug.dump($basic);

        $composite : 2 byte = mem.sizeof({ 8 byte, 8 byte });
        debug.dump($composite);
    } return;
}
