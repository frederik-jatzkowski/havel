
declare (
    one : 1 byte = 0b1,
    two : 1 byte = 0b10,
    result : 1 byte = 0b0,
    indirect : 1 byte = mem.ptr(result)
);

func main() {
    block entry {
        $result : 1 byte = alu.add_u(one, two);
        mem.store(indirect, $result);
        debug.dump(result);
    } return;
}
