
declare (
    one : 1 byte = 0b1,
    two : 1 byte = 0b10,
    result : 1 byte = 0b0
);

func main() {
    block entry {
        result = alu.add_u(one, two);
        debug.dump(result);
    } return;
}
