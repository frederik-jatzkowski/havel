
func main() {
    block entry {
        $size : 8 byte = 0b1;
        $ptr : ref = mem.alloc($size);
        $one : 1 byte = 0b1;
        mem.store($ptr, $one);
        $loaded : 1 byte = mem.load($ptr);
        mem.free($ptr);
        debug.dump($loaded);
    } return;
}
