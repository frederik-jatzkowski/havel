
func main() {
    block entry {
        $size : 4 byte = 0b1000;
        $ptr : ref = mem.alloc($size);
        mem.store($ptr, $size);
        $loaded : 4 byte = mem.load($ptr);
        debug.dump($loaded);
    } return;
}
