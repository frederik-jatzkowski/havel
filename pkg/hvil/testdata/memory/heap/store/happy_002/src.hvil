
func main() {
    block entry {
        $size : 1 byte = 0b1000;
        $ptr : ref = mem.alloc($size);
        mem.store($ptr, $size);
        $loaded : 1 byte = mem.load($ptr);
        debug.dump($loaded);
    } return;
}
